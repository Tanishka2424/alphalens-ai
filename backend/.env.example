APP_NAME=AlphaLens AI
APP_VERSION=0.1.0
ENV=development
LOG_LEVEL=INFO
FINBERT_MODEL_NAME=ProsusAI/finbert
DEVICE=cpu

# Used starting Phase 4 (consensus reasoning). Leave blank for now — the
# fallback rule-based consensus system works without it.
OPENAI_API_KEY=
